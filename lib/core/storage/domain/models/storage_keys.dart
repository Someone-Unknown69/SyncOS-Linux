class StorageKeys {
  // auth key
  static const String pairingToken = 'auth_token';
  
  // saved client config
  static const String clientConfig = 'client_config';

  // server config
  static const String serverConfig = 'server_config';

  // settings
  static const String appSettings = 'app_settings';
}